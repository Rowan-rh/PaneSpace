## 1. 焦点框

- [x] 1.1 在列表和分栏内容区关闭焦点效果；在 App 中确认 Tab 切换后没有外层框，顶部强调线仍在。

## 2. 显示结果缓存

- [x] 2.1 为面板内容和各列增加内容版本，实现按（版本、搜索、排序）缓存的 `visibleItems` 和 `displayedItems(in:)`，并替换视图与键盘路径中的调用；用测试验证搜索、排序变化后结果更新，内容变化后缓存失效。
- [x] 2.2 重新采样在 `/usr/bin` 中连续按方向键的主线程耗时，确认排序不再是热点。

## 3. 加载提示

- [x] 3.1 新增延迟加载提示并用于视图；刷新当前目录时不隐藏内容；用测试验证快速加载不会点亮 `showsLoadingIndicator`，同目录刷新保持内容。

## 4. 验证

- [x] 4.1 运行 `swift test`、`make app`、严格签名检查和 `openspec validate pane-performance-polish --strict`，并在 App 中实测导航、刷新与搜索动画。

验证记录：`swift test` 共 70 项通过；`make app`、严格签名检查和 `openspec validate --strict` 通过。修复前，在 `/usr/bin`（932 项）的分栏中连续按 60 次方向键，6 秒采样里约 2.6 秒的主线程时间在 `displayedItems(from:)` 排序上；修复后同样的操作里不再出现排序或视图计算热点，主线程基本处于空闲等待。App 中确认 Tab 切换后没有外层焦点框，活动面板顶部强调线仍在。实测中发现分栏用方向键移动选择时不会自动滚动，已让列表跟随选中项滚动并复测通过。
