## 1. 模型与执行

- [x] 1.1 `BatchRenameRule` 与 `BatchRenamePlan`：三种规则、扩展名拆分、无效名称、批次内重名与现有项目冲突检查。
- [x] 1.2 `BatchRenamer`：临时名两阶段执行与失败回滚。
- [x] 1.3 `BrowserPaneModel` 接入请求、预览与执行；刷新后按标准化路径保留选择。
- [x] 1.4 单元测试：规则、冲突、互换与大小写改名、失败回滚、面板执行后选择结果。

## 2. 界面

- [x] 2.1 批量重命名表单（方式切换、字段、实时预览、问题摘要）；右键菜单与“文件 > 重命名…”入口；中文本地化。

## 3. 验证

- [x] 3.1 `swift test`、`make app`、签名检查、`openspec validate batch-rename --strict`。
- [x] 3.2 在 macOS 上实测。

验证记录：`swift test -Xswiftc -warnings-as-errors` 全部通过；`make app` 与严格签名检查通过。在隔离配置的开发版（单面板、分栏视图）中实测：多选三个文件后从“文件 > 重命名…”打开表单；替换文本产生与未选中文件同名时标红并禁用按钮；切换到格式模式改为“Note 01/02/03.txt”后执行成功并自动选中新名称。列表视图的右键入口与双面板布局共用同一菜单和表单，未逐项点按。
