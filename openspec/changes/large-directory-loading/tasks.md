## 1. 实现

- [x] 1.1 性能测量：定位“种类”查询、名称推导与排序闭包开销。
- [x] 1.2 `contentBatches` 协议要求与本地分批枚举；“种类”缓存；`FileItem.name` 存储。
- [x] 1.3 `FileItemSorter`；面板分批显示、限频合并、后台排序并预填显示缓存；状态栏载入提示。
- [x] 1.4 测试：排序规则、分批与隐藏文件、种类共享、缺失文件夹错误、首批先显示。
- [x] 1.5 ADR 0010、架构文档、路线图与功能清单。

## 2. 验证

- [x] 2.1 `swift test`、`make app`、签名检查、`openspec validate large-directory-loading --strict`。
- [x] 2.2 在 macOS 上实测。

验证记录：`swift test -Xswiftc -warnings-as-errors` 共 119 项通过；`make app` 与严格签名检查通过。发布构建下测量 5 万个文件：读取 2.9 秒降至 0.77 秒，排序 2.5 秒降至 0.17 秒。在隔离配置的开发版双面板分栏视图中，通过侧栏工作区打开 5 万个文件的文件夹：先显示首批项目，随后显示“50,000 个项目”的完整排序列表，界面未停顿。
