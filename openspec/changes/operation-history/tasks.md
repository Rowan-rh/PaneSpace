## 1. 实现

- [x] 1.1 记录模型、JSON 存储与历史模型（上限、串行写入、损坏文件回退）。
- [x] 1.2 废纸篓位置回传；传输队列按运行汇报；面板操作记录。
- [x] 1.3 操作历史窗口、菜单入口与传输栏入口；中文本地化。
- [x] 1.4 测试：存储往返与损坏、上限与重新加载、队列汇报废纸篓位置、面板记录。
- [x] 1.5 ADR 0008、架构文档、路线图与功能清单。

## 2. 验证

- [x] 2.1 `swift test`、`make app`、签名检查、`openspec validate operation-history --strict`。
- [x] 2.2 在 macOS 上实测。

验证记录：`swift test -Xswiftc -warnings-as-errors` 共 104 项通过；`make app` 与严格签名检查通过。在隔离配置的开发版双面板中新建文件夹后，从“传输 > 操作历史…”看到“新建文件夹“New Folder””记录；重启应用后记录仍在；中文界面检查通过（补充了“完成”按钮翻译）。
