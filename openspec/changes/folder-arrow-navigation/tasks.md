## 1. 文件夹导航与选择恢复

- [x] 1.1 为活动列表的唯一选中目录增加右箭头导航入口；为左箭头上级导航记录离开目录的 URL，并在加载完成后恢复对应文件夹选择；用模型回归测试验证进入、返回选中及文件/多选/根目录无操作。
- [x] 1.2 为分栏视图增加当前列上下选择、右箭头进入子列和左箭头返回上一列的模型行为，并覆盖空列、加载中和文件选择。

## 2. 列表与分栏键盘交互

- [x] 2.1 仅在文件列表获得焦点时处理左右方向键，保留上下键选择和文本输入行为。
- [x] 2.2 在 macOS 单面板中实测左右键连续进入/返回、上下键选择，以及搜索框和路径输入框的左右光标行为。
- [x] 2.3 在 macOS 双面板中确认只有当前聚焦面板响应左右方向键。
- [x] 2.4 为分栏视图的每一列维护键盘焦点；点击或上下键选择后，按右方向键进入子列、按左方向键返回父列。
- [x] 2.5 在 macOS 单面板中实测列表和分栏两种视图的鼠标选中后方向键导航、空目录和文本输入行为。
- [x] 2.6 在 macOS 双面板中实测列表和分栏两种视图，并确认只有当前聚焦面板响应方向键。

验证记录：PaneSpace 单面板与双面板下，列表和分栏视图都已验证鼠标选中后按右方向键进入文件夹、按左方向键返回。分栏测试覆盖空文件夹和加载中的列；双面板中非活动面板保持原位置。路径栏和搜索框的方向键输入行为已验证不受影响。新增列选择、进入、返回、空列、加载中和文件无操作模型测试，46 项测试全部通过。

## 3. 集成验证

- [x] 3.1 运行 `swift test`、`make app`、严格签名检查和 `openspec validate --strict`；启动应用验证真实键盘导航及根目录边界。
- [x] 3.2 实现后重跑自动化验证和 OpenSpec 严格检查，启动当前构建的 PaneSpace 并完成单/双面板实测。
