## 1. 文件夹导航与选择恢复

- [x] 1.1 为活动列表的唯一选中目录增加右箭头导航入口；为左箭头上级导航记录离开目录的 URL，并在加载完成后恢复对应文件夹选择；用模型回归测试验证进入、返回选中及文件/多选/根目录无操作。

## 2. 列表键盘交互

- [x] 2.1 仅在文件列表获得焦点时处理左右方向键，保留上下键选择和文本输入行为。
- [x] 2.2 在 macOS 单面板中实测左右键连续进入/返回、上下键选择，以及搜索框和路径输入框的左右光标行为。
- [x] 2.3 在 macOS 双面板中确认只有当前聚焦面板响应左右方向键。

验证记录：单面板和双列面板均已用临时测试目录验证鼠标选中后按右箭头进入、按左箭头返回并恢复选中。双面板验证中右侧目录按预期变化，左侧仍停留在原目录；测试后已恢复用户原来的单面板目录与分栏视图。

## 3. 集成验证

- [x] 3.1 运行 `swift test`、`make app`、严格签名检查和 `openspec validate --strict`；启动应用验证真实键盘导航及根目录边界。
