## 1. 实现可收起的搜索交互

- [x] 1.1 空闲时以带无障碍名称的放大镜按钮替代文本框；验证每个空搜索词面板默认收起。
- [x] 1.2 点击按钮或在活动面板按 Command-F 时展开并聚焦搜索框；验证两种入口都聚焦到正确面板。
- [x] 1.3 添加清除按钮，清除该面板搜索词并收起搜索框。
- [x] 1.4 保持每个面板独立实时过滤，并在焦点移开后保留搜索词；验证一个面板的查询不影响另一个面板结果。
- [x] 1.5 添加或更新搜索按钮及清除/关闭按钮的本地化名称和无障碍帮助；验证英文和简体中文 VoiceOver 名称已定义。

## 2. 验证与说明

- [x] 2.1 为不区分大小写的面板过滤和面板隔离添加回归测试；运行 `swift test` 验证，UI 展开/聚焦/清除由实机检查覆盖。
- [x] 2.2 运行 `make app` 和严格签名校验；启动应用并检查单面板、双面板和最小窗口宽度下的搜索交互。
- [x] 2.3 运行 `openspec validate --strict`；若最终交互需要用户说明，更新相应使用或开发文档。
